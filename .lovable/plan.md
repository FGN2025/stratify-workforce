
# Commit Surface 2 contract docs

Drop the two markdown files from `fgn-academy-api-docs.zip` into `docs/api/`. No code, schema, or function changes.

## Files to add
- `docs/api/attach-assessment-to-workorder.md` — confirmed request/response contract for the assessment-attach endpoint (the doc the original DEV ORDER requested).
- `docs/api/import-challenge-as-workorder.md` — companion contract for the import endpoint, so both halves of the Surface 2 bridge are documented in one place.

## How
Unzip `user-uploads://fgn-academy-api-docs.zip` and copy the two `.md` files verbatim to `docs/api/`. The archive contains only those two files under `docs/api/` (verified — no `.git`, no other paths). Existing files in `docs/api/` are untouched.

## Acceptance
- `docs/api/attach-assessment-to-workorder.md` and `docs/api/import-challenge-as-workorder.md` exist with the uploaded content.
- No other files modified.
